/**
 * This a temporary solution. I need to rewrite this myself.
 * @param grid
 * @param rowStart
 * @param colStart
 * @param num
 * @returns
 */
export declare function sudokuGenerator(k: number): {
    grid: number[][];
    solution: Map<any, any>;
};
